/**
 * Product Class
 */
export default class Product {
  /**
   * constructor
   * @param id
   * @param name
   * @param price
   */
  constructor(id, name, price) {
    this.id = id;
    this.name = name;
    this.price = price;
  }
}
