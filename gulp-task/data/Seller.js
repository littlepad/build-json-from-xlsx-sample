/**
 * Seller Class
 */
export default class Seller {
  /**
   * constructor
   * @param id
   * @param name
   * @param tel
   */
  constructor(id, name, tel) {
    this.id = id;
    this.name = name;
    this.tel = tel;
  }
}
